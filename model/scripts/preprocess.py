import pandas as pd
import numpy as np

def load_data(filepath):
    print(f"Loading data from {filepath}")
    # return pd.read_csv(filepath)
    pass

def preprocess_interactions(df):
    print("Preprocessing interactions...")
    # Clean data and keep relevant columns
    pass

def build_features(df):
    print("Building user and item features...")
    pass

if __name__ == "__main__":
    print("Running preprocess.py")
    # df = load_data('raw_dataset.csv')
    # clean_df = preprocess_interactions(df)
    # clean_df.to_csv('../outputs/clean_interactions.csv', index=False)

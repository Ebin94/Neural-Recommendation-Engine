import pandas as pd
import numpy as np
# import tensorflow as tf

def train_model(interactions_df):
    print("Training offline recommender model...")
    # Implement Two-Tower or Matrix Factorization model
    pass

def save_model(model, filepath):
    print(f"Saving model to {filepath}")
    pass

if __name__ == "__main__":
    print("Running train_recommender.py")
    # df = pd.read_csv('../outputs/clean_interactions.csv')
    # model = train_model(df)
    # save_model(model, '../outputs/recommender_model')
